set(PROJECT_VERSION 1.4)

cmake_minimum_required(VERSION 3.10...3.30)
project(collatz_hypothesis VERSION ${PROJECT_VERSION})

add_subdirectory(include/Console_UI)

set(SRC
    src/main.cpp
    src/algorithm_start/get_numbers.cpp
    src/algorithm_start/algorithm_start.cpp
    src/arguments.cpp
)

add_executable(program ${SRC})

target_link_libraries(program PRIVATE console_ui)

target_compile_definitions(program PRIVATE
    PROGRAM_VERSION=${PROJECT_VERSION}
)