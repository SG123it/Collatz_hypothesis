#include <iostream>

#include "arguments.hpp"

void arguments(int argc, char* argv[], algorithm_start start_settings) {
    std::vector<std::string> argv_c;
    for (int i = 0; i < argc; i++) argv_c.push_back(argv[i]);

    


}
